{
  "name": "ship-ops",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "triage": "tsx src/cli.ts",
    "triage:pr": "tsx src/triage-pr.ts",
    "test": "node --import tsx --test test/*.test.ts",
    "check": "tsc --noEmit && npm test"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
